/**
 * This gradle file contains the very basic dependencies for each module
 */

dependencies {
    apply from: '../config/configDependenciesGroup.gradle'
    kotlin()
    dagger()
    networking()
    rx()
    architectureComponents()
    view()
    testing()
}